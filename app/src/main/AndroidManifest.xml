<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.todo.AddToDoActivity"></activity>
        <activity android:name=".ui.main.activities.DocumentActivity" />
        <activity android:name=".ui.main.todo.ToDoActivity" />
        <activity android:name=".ui.main.activities.CollectRentActivity" />
        <activity android:name=".ui.main.activities.TransactionActivity" />
        <activity android:name=".ui.main.activities.TenantActivity" />
        <activity android:name=".ui.main.activities.PropertyActivity" />
        <activity android:name=".ui.main.activities.HomeActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.main.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>